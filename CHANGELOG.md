## v0.1.0-android-demo (2025-11-12)
- End-to-end demo: paid → claim → PRP → bundle → auto-status
- Hardware-wrapped DID keys (Keystore/StrongBox fallback-safe)
- Paid-gated prove flow (preimage + preimage_sha256 in claim)
- PRP builder + inclusion in bundle
- did_sig (ES256K) at top-level payload
- UI: copy Login ID, Reset session, auto-status fetch after send
